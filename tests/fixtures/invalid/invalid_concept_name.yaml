# T049: Triggers HOOK-004 - Hook concept must be lower_snake_case
manifest_version: "1.0.0"
schema_version: "1.0.0"

frames:
  - name: "psa.customer"
    source:
      relation: "psa.customer"
    hooks:
      - name: "_hk__customer"
        role: "primary"
        concept: "Customer"  # Invalid: not lower_snake_case -> HOOK-004
        source: "CRM"
        expr: "customer_id"

concepts:
  - name: "Customer"
    description: "A customer entity for testing"
