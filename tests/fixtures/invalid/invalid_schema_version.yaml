# T057: Triggers MANIFEST-002 - schema_version must be valid semver
manifest_version: "1.0.0"
schema_version: "1.0"  # Invalid: missing patch version -> MANIFEST-002

frames:
  - name: "psa.customer"
    source:
      relation: "psa.customer"
    hooks:
      - name: "_hk__customer"
        role: "primary"
        concept: "customer"
        source: "CRM"
        expr: "customer_id"

concepts:
  - name: "customer"
    description: "A customer entity for testing"
