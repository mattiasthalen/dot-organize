# T046: Triggers HOOK-001 - Hook must have required fields
manifest_version: "1.0.0"
schema_version: "1.0.0"

frames:
  - name: "psa.customer"
    source:
      relation: "psa.customer"
    hooks:
      - name: "_hk__customer"
        role: "primary"
        # Missing: concept, source, expr -> HOOK-001

concepts: []
