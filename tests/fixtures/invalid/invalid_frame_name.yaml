# T041: Triggers FRAME-002 - Frame name must match <schema>.<table> lower_snake_case
manifest_version: "1.0.0"
schema_version: "1.0.0"

frames:
  - name: "Customer"  # Invalid: not lower_snake_case, missing dot
    source:
      relation: "psa.customer"
    hooks:
      - name: "_hk__customer"
        role: "primary"
        concept: "customer"
        source: "CRM"
        expr: "customer_id"

concepts:
  - name: "customer"
    description: "A customer entity for testing"
