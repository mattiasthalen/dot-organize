# T051: Triggers HOOK-006 - Hook expr must be non-empty
manifest_version: "1.0.0"
schema_version: "1.0.0"

frames:
  - name: "psa.customer"
    source:
      relation: "psa.customer"
    hooks:
      - name: "_hk__customer"
        role: "primary"
        concept: "customer"
        source: "CRM"
        expr: ""  # Empty expression -> HOOK-006

concepts:
  - name: "customer"
    description: "A customer entity for testing"
