# T056: Triggers MANIFEST-001 - manifest_version must be valid semver
manifest_version: "v1.0"  # Invalid: 'v' prefix and missing patch -> MANIFEST-001
schema_version: "1.0.0"

frames:
  - name: "psa.customer"
    source:
      relation: "psa.customer"
    hooks:
      - name: "_hk__customer"
        role: "primary"
        concept: "customer"
        source: "CRM"
        expr: "customer_id"

concepts:
  - name: "customer"
    description: "A customer entity for testing"
