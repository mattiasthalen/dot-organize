# T055: Triggers CONCEPT-002 - Concept description must be 10-200 characters
manifest_version: "1.0.0"
schema_version: "1.0.0"

frames:
  - name: "psa.customer"
    source:
      relation: "psa.customer"
    hooks:
      - name: "_hk__customer"
        role: "primary"
        concept: "customer"
        source: "CRM"
        expr: "customer_id"

concepts:
  - name: "customer"
    description: "Short"  # Too short (< 10 chars) -> CONCEPT-002
