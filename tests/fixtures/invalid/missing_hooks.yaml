# T040: Triggers FRAME-001 - Frame must have at least one hook
manifest_version: "1.0.0"
schema_version: "1.0.0"

frames:
  - name: "psa.customer"
    source:
      relation: "psa.customer"
    hooks: []  # Empty hooks list triggers FRAME-001

concepts: []
