# T052: Triggers HOOK-006 - Forbidden pattern SELECT in expr
manifest_version: "1.0.0"
schema_version: "1.0.0"

frames:
  - name: "psa.customer"
    source:
      relation: "psa.customer"
    hooks:
      - name: "_hk__customer"
        role: "primary"
        concept: "customer"
        source: "CRM"
        expr: "SELECT customer_id FROM customers"  # Forbidden SELECT -> HOOK-006

concepts:
  - name: "customer"
    description: "A customer entity for testing"
