# T057a: Triggers MANIFEST-W02 - Unknown fields for forward compatibility
manifest_version: "1.0.0"
schema_version: "1.0.0"

# Unknown field at root level -> MANIFEST-W02
future_feature: true
experimental_options:
  enabled: true

frames:
  - name: "psa.customer"
    source:
      relation: "psa.customer"
    hooks:
      - name: "_hk__customer"
        role: "primary"
        concept: "customer"
        source: "CRM"
        expr: "customer_id"

concepts:
  - name: "customer"
    description: "A customer entity for testing"
